//
//  ImageRetreiver.swift
//  SniprSwarm
//
//  Created by Emanuel Mairoll on 14.05.21.
//

import Foundation
import Combine
import SwiftUI

class ImageRetreiver {

    /*
    public static func retreiveThumbnail(forUrl url: URL){
        URLSession.shared.dataTaskPublisher(for: URLRequest(url: url, cachePolicy: .returnCacheDataElseLoad))
            .tryMap { (data, response) -> <#Result#> in
                if (response as? HTTPURLResponse)?.statusCode == 200, let image = UIImage(data: data) {
                    return image
                } else {
                    return UIImage()
                }
            }
    }
    */

}
